#ifndef DISTINCT_H
#define DISTINCT_H
#include "include/header.h"

class Distinct
{
    public:
        Distinct();
        virtual ~Distinct();
        int numDistinct(string S, string T);
    protected:
    private:
};
void test_distict();
#endif // DISTINCT_H
