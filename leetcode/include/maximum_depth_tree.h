#ifndef MAXIMUM_DEPTH_TREE_H
#define MAXIMUM_DEPTH_TREE_H
#include "treenode.h"

class Maximum_Depth_Tree
{
    public:
        Maximum_Depth_Tree();
        virtual ~Maximum_Depth_Tree();
        int maxDepth(TreeNode *root);
    protected:
    private:
};

#endif // MAXIMUM_DEPTH_TREE_H
